# Allow peer dependency resolution for @react-three/fiber@9.x
# R3F v9.7.0 declares peer "react": ">=19 <19.3" but resolves fine with 19.2.8
# Without this, `npm ci` in GitHub Actions fails with ERESOLVE
legacy-peer-deps=true
